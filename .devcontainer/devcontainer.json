{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "type=bind,source=${localEnv:HOME}/.config/helix,destination=/root/.config/helix"
  ],
  "remoteEnv": {
    "PATH": "${containerEnv:HOME}/go/bin:${containerEnv:PATH}"
  }
}
