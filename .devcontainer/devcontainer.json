{
  // "image": "${localEnv:MYIMAGE_NAME}:${localEnv:MYIMAGE_LABEL}"
  "remoteEnv": {
    "PATH": "${containerEnv:HOME}/bin:${containerEnv:PATH}"
  },
  "build": {
    "dockerfile": "Dockerfile"
  }
  // "dockerComposeFile": "docker-compose.yml",
  // "service": "app"
}
