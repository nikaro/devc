---

version: "3"

services:
  app:
    build:
      context: ".."
      dockerfile: ".devcontainer/Dockerfile"
    user: "code"
    volumes:
      # workspace
      - "..:/workspace"
      # dotfiles
      - "$HOME/.vimrc:/home/code/.vimrc"
      - "$HOME/.git:/home/code/.git"
      - "$HOME/.ssh:/home/code/.ssh"
      - "$HOME/.gnupg:/home/code/.gnupg"
      - "$SSH_AUTH_SOCK:/ssh-agent"
    environment:
      - "SSH_AUTH_SOCK=/ssh-agent"
